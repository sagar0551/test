global:
  scrape_interval: 15s  # Scrape targets every 15 seconds

scrape_configs:
  - job_name: 'github_actions_metrics'
    static_configs:
      - targets: ['localhost:8000']  # Replace with the actual host and port if different

# Data retention settings
storage:
  retention: 30d  # Retain data for 30 days